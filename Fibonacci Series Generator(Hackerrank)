#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
     long long n;
    if (scanf("%lld", &n) != 1) return 0;
    if (n <= 0) {                           
        printf("\n");
        return 0;
    }
    long long a = 0, b = 1;
    for (long long i = 0; i < n; ++i) {
        if (i == 0) {
            printf("%lld", a);
        } else if (i == 1) {
            printf(" %lld", b);
        } else {
            long long c = a + b;
            printf(" %lld", c);
            a = b;
            b = c;
        }
    }

    printf("\n");
     
    return 0;
}
