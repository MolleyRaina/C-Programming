#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main() {
    int N;
    scanf("%d", &N);
    int arr[N]; 
    int *ptr = arr; 
    for (int i = 0; i < N; i++) {
        scanf("%d", (ptr + i));
    }
    for (int i = 0; i < N; i++) {
        printf("%d", *(ptr + i)); 
        if (i < N - 1) {
            printf(" ");
        }
    }
    printf("\n");
    return 0;
}
