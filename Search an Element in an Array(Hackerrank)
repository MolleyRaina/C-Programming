#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,i;
    scanf("%d",&n);
    int arr[n];
    for(i=0;i<n;i++){
        scanf("%d",&arr[i]);
    } 
    int val;
    scanf("%d",&val);
    int index=-1;
    for(i=0;i<n;i++){
        if(arr[i]==val){
            index=i;
            break;
        }
    }
    if(index==-1)
        printf("Element not found");
    else 
        printf("Element found at position %d",index+1);
    return 0;
}
